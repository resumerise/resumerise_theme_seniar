<div class="key-value-item" id="<%= it.id %>">
<% if(it.label) { %>
   <label class="information">
        <%= it.label %>
    </label>
<% } %>
<% if(it.value) { %>
     <span class="key__info">
        <%= it.value %>
    </span>
<% } %>
</div>